# Weave Protocol — ARCHIVED (superseded)

**This document is non-normative.** The Weave Protocol now lives at
[`.claude/skills/weave/`](.claude/skills/weave/SKILL.md) (SKILL.md +
`hooks/`), rebuilt on real Claude Code primitives — subagent fan-out, git
worktree isolation, and an opt-in PreToolUse scope-guard hook — under the
META v3 core charter. It activates only by explicit user invocation
(`/weave`), never by trigger phrases in task text.

The v0.2 text (charter rule ZPR5's mechanics appendix) is preserved in git
history: `git log -- WEAVE-PROTOCOL.md`. Do not cite this file as protocol
authority, and do not ship it to eval arms — the weave eval contrast is
skill-present vs skill-absent (DECISIONS #5).
